# WebSocket upgrade map for HMR
map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

# Allow larger request bodies (ZIP uploads, etc.)
client_max_body_size 200m;

# Include project-specific subdomain configs
include /etc/nginx/conf.d/projects/*.conf;

server {
    listen 80;
    server_name localhost;
    # Allow larger uploads (ZIP uploads go through /api/*)
    client_max_body_size 110m;

    # Backend API Proxy
    location /api/ {
        proxy_pass http://backend:5000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Frontend Proxy
    location / {
        proxy_pass http://frontend:5173;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ------------------------------------------------------------
# WILDCARD SUBDOMAINS for static sites (local dev)
# Note: Subdomains won't work on plain localhost.
# For local subdomain testing, use localhost.sslip.io or add entries to hosts file
# ------------------------------------------------------------
# server {
#     listen 80;
#     server_name ~^(?<slug>.+)\.localhost$;
#
#     root /srv/static-sites/$slug/current;
#     index index.html;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
# }
